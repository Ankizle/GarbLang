fn <test> {

    var <shared>

    fn <ret> {
        return <shared>
    }

    return <ret>
}

fn <main> {
    var <a>
    var <b>
    var <c>
    var <one>

    set <c>, [0], [-100000]
    set <one>, [0], [1]

    lbl <label>

    add <c>, <one>

    cal <test>, <a>
    calp <a>, <b>

    jmpls <c>, <label>
}